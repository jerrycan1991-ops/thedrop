{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infrastructure/docker/Dockerfile.api"
  },
  "deploy": {
    "startCommand": "cd /app/services/worker && exec celery -A app.celery_app worker -B -Q ingest,maintain,publish --concurrency=2 --loglevel=INFO --without-gossip --without-mingle",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
